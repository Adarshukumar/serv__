{
  "name": "aduskills-chat",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "React SPA that calls each LLM provider's real API directly from the browser. No hosted server, no relay, no Python.",
  "author": "Adarsh",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "start": "vite",
    "test": "node --import tsx --test tests/*.test.ts",
    "typecheck": "tsc --noEmit",
    "gen:models": "python3 scripts/dump_registry.py",
    "bridge:fallback": "node bridge/server.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "tsx": "^4.23.15",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  },
  "engines": {
    "node": ">=20"
  }
}
