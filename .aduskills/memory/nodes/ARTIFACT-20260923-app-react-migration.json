{
  "id": "ARTIFACT-20260923-app-react-migration",
  "type": "ARTIFACT",
  "title": "app/ — React SPA + local egress bridge replacing the Python server",
  "summary": "The migration deliverable at /home/user/serv__/app/. 32 tracked files: a Vite + React 18 + TypeScript single-page chat UI, a zero-dependency Node bridge that dials the providers, generated model data, three test suites, and ARCHITECTURE.md. The Python server was not modified.",
  "facts": [
    {
      "claim": "32 files tracked under app/ (node_modules and dist correctly excluded by the new root .gitignore). Roughly 4,512 hand-written lines of TS/TSX/MJS/Python/CSS plus 689 generated lines in src/data/models.ts.",
      "classification": "TOOL_VERIFIED",
      "evidence": ["find app -type f excluding node_modules/dist -> 32", "git add -An app/ | wc -l -> 32", "wc -l over source files"],
      "confidence": "high"
    },
    {
      "claim": "Stack: Vite 5 + React 18.3 + TypeScript 5.6, tsx for tests, @types/node. Production bundle 192.47 kB JS (58.82 kB gzip) and 13.11 kB CSS (3.48 kB gzip), 42 modules, built in 1.26s. No UI framework, no state library, no markdown library.",
      "classification": "TOOL_VERIFIED",
      "evidence": ["npx vite build output", "package.json"],
      "confidence": "high"
    },
    {
      "claim": "Bridge has ZERO runtime dependencies - only node:http plus local modules. Requires Node >=20 for global fetch and ReadableStream. Binds 127.0.0.1:8787 by default.",
      "classification": "OBSERVED",
      "evidence": ["bridge/server.mjs import list", "package.json dependencies contain only react and react-dom"],
      "confidence": "high"
    },
    {
      "claim": "Model data is GENERATED, never hand-typed. scripts/dump_registry.py executes the real Models.py (stdlib-only so it runs with no dependencies installed) and emits src/data/models.ts. Output: 64 registry models -> 32 after dropping DevsDo-exclusive entries, plus 18 recovered DeepInfra models = 50 across 6 providers. LLMChat @cf/@hf tags joined 24/24. Upstage ids asserted against v3 _MODELS 4/4.",
      "classification": "TOOL_VERIFIED",
      "evidence": ["python3 scripts/dump_registry.py output"],
      "confidence": "high"
    },
    {
      "claim": "Tests: 60 total, 60 pass, 0 fail in about 4.7s via node --test with tsx. Split as 47 unit tests over the four wire formats, the SSE framer and ThinkSplitter; 3 differential tests covering 815 cases generated by executing the original Python ThinkSplitter; 10 integration tests that spawn the real bridge on port 8799 and drive it through the real browser-side modules.",
      "classification": "TOOL_VERIFIED",
      "evidence": ["npm test -> '# tests 60 # pass 60 # fail 0'", "scripts/gen_thinksplitter_fixtures.py -> 815 cases, lossless invariant 400/400"],
      "confidence": "high"
    },
    {
      "claim": "npx tsc --noEmit exits 0 with no output under strict mode plus noUnusedLocals, noUnusedParameters, noImplicitOverride and verbatimModuleSyntax.",
      "classification": "TOOL_VERIFIED",
      "evidence": ["npx tsc --noEmit -> exit 0, empty"],
      "confidence": "high"
    },
    {
      "claim": "Live pipeline verified against the running bridge THROUGH the Vite proxy (the exact path a browser uses): GET /bridge/health -> 200 with all 6 providers plus mock, DevsDo absent, credentials {Upstage:false, Mercury:false}. GET / -> 200. POST mock -> event:meta then event:raw frames streaming. POST Upstage -> meta then error{code:missing_credentials} then end. POST DevsDo -> 404. POST malformed JSON -> 400.",
      "classification": "TOOL_VERIFIED",
      "evidence": ["curl probes against 127.0.0.1:5173 and 127.0.0.1:8787"],
      "confidence": "high"
    },
    {
      "claim": "Architecture: four provider wire formats (openai-delta, workers-raw, reasoning-delta, typed-events) plus upstage-v3 normalise to a single StreamEvent union (thinking|content|source|usage|status|done|error). The UI renders only StreamEvent and never branches on provider, so a fifth format needs one normaliser and zero UI changes.",
      "classification": "OBSERVED",
      "evidence": ["src/types.ts StreamEvent", "src/lib/normalizers.ts createNormalizer", "src/components/Message.tsx has no provider switch"],
      "confidence": "high"
    },
    {
      "claim": "NOT DONE and not claimed: no live call against any real provider (sandbox TLS egress blocked, curl exit 35 for all six hosts); no real-DOM browser E2E (no browser, no docker); Upstage/Mercury credential capture not ported; Dolphin attachment UI not wired; no security review (deferred by user instruction).",
      "classification": "TOOL_VERIFIED",
      "evidence": ["cors_probe2.sh exit codes", "command -v docker -> MISSING", "ARCHITECTURE.md section 10"],
      "confidence": "high"
    },
    {
      "claim": "The Python server is untouched. git diff on 'My PREVIOUS ENTIRE SERVER/' and 'New Upstage Change Logs/' is empty; all changes are new paths under app/, .aduskills/, .gitignore and AGENTS.md.",
      "classification": "TOOL_VERIFIED",
      "evidence": ["git status --porcelain shows only M on .aduskills/state/skills-route.md and new untracked paths"],
      "confidence": "high"
    }
  ],
  "edges": [
    {"type": "IMPLEMENTS", "target": "DECISION-20260923-local-egress-bridge"},
    {"type": "SUPERSEDES", "target": "PROJECT-20260923-serv-llm-gateway 'My PREVIOUS ENTIRE SERVER/' - PROPOSED, the Python tree still exists unmodified"},
    {"type": "DERIVED_FROM", "target": "ARTIFACT-20260923-upstage-v3"},
    {"type": "VERIFIED_BY", "target": "npm test (60/60), tsc --noEmit, vite build, live curl probes"},
    {"type": "RELATED_TO", "target": "LEARNING-20260923-verify-dont-infer"},
    {"type": "AFFECTS", "target": "RISK-20260923-runtime-unverified"}
  ],
  "source": "react migration work, 2026-09-23",
  "sensitivity": "internal",
  "retention": "project",
  "status": "active",
  "created_at": "2026-09-23",
  "updated_at": "2026-09-23",
  "last_verified_at": "2026-09-23"
}
